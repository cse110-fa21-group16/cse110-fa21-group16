name: code-coverage


on: ["push"]

jobs:
  code-coverage:
    runs-on: ubuntu-latest
    name: code-coverage
    steps:
      - uses: ziishaned/jest-reporter-action@v0.0.1
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
